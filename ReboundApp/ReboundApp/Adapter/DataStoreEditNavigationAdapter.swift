//
//  DataStoreEditNavigationAdapter.swift
//  ReboundApp
//
//  Created by Ethan Keiser on 3/12/22.
//

import Foundation
import Rebound

class DataStoreEditNavigationAdapter { //RBUrlStore, RBUserStore {
    /*

    func deleteRBUser(rbUserId: String, completion: @escaping DeleteCompletion) {
        self.rbUserStore.deleteRBUser(rbUserId: rbUserId) { result in
            switch result {
            case .failure(let error)
            }
            editnavigation.navigateToSuccessDelete()
        }
    }
    func retrieve(completion: @escaping RetrieveCompletion) {
        <#code#>
    }
    
    let rbUserStore : RBUserStore
    let editnavigation: EditControllerNavigation
    init (rbUser: RBUserStore, editUserNavigation: EditControllerNavigation) {
        self.rbUserStore = rbUserStore
        self.editnavigation = editUserNavigation
    }
    func deleteRBUrl(rbUrl urlId: String, completion: @escaping DeletionCompletion) {
        
    }
    
    func insert(rbUrl item: LocalRBUrl, userId: String, completion: @escaping (Result<LocalRBUrl, Error>) -> ()) {
        <#code#>
    }
    
    func insert(rbUrl item: [LocalRBUrl], user: LocalRBUser, timestamp: Date, completion: @escaping InsertionCompletion) {
        <#code#>
    }
    
    func retrievePrimary(userId: String, completion: @escaping RetrievalCompletion) {
        <#code#>
    }
    
    func retrieve(userId: String, count: Int, offset: Int, completion: @escaping RetrievalCompletion) {
        <#code#>
    }
    
    
    */
}
